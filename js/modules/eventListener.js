// mes imports
import { body, newH1, newInputText, newInputButton, buttonAll, buttonFini, buttonPasFini, divInput, divButton } from "./newElements.js";

newInputButton.addEventListener("click", () => {
    if (newInputText.value.length == 0) {
        
    }
});
